server:
  port: 8070

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_SERVER_ADDRESS:http://localhost:8070/eureka}
    registerWithEureka: false
    fetchRegistry: false
  instance:
    hostname: localhost
