server:
  port: 8070

eureka:
  client:
    serviceUrl:
      defaultZone: http://{eurekaServer.instance.hostname}:${server.port}/eureka/
    registerWithEureka: false
    fetchRegistry: false
  instance:
    hostname: localhost
